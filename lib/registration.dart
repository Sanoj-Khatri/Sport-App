import 'package:flutter/material.dart';

class Registartaion extends StatelessWidget {
  const Registartaion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
